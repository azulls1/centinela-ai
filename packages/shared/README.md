# Shared Package

Este paquete contiene tipos y esquemas compartidos entre frontend y backend.

Por ahora, los tipos están definidos en el frontend (`apps/web/src/types.ts`), pero en el futuro se pueden mover aquí para compartirlos.

